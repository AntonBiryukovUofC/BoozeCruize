version: '3'
services:
  nominatim:
    build: nominatum/.
    ports: 
      - "6432:5432"
      - "7070:8080"
    volumes:
      - nominatum:/var/lib/postgresql/11/main
    entrypoint:
      - /app/start.sh
volumes:
  nominatum: